Compile with -lpthread flag (e.g. gcc p2p.c -o p2p -lpthread).

Pass to executable file next parameters:
1. Your ip.
2. Your port.
3. Ip of peer to connect (pass if your peer is not the first in the system).
4  Port of peer to connect (pass if your peer is not the first in the system).
